; Basic arithmetic with infix operators and one numeric helper.
(let ((a 7) (b 5))
  (println
    (list
      'sum a + b
      'difference a - b
      'product a * b
      'quotient 20 / 4
      'root sqrt(81))))
